---
all:
  children:
    azure_hosts:  
      hosts:
        devops-vm:  #devops_vm
          ansible_host: 74.241.130.212  # Your actual server IP
          ansible_user: azureuser     # Your SSH username
          ansible_ssh_private_key_file: ~/.ssh/id_boostflow  # Path to your SSH key
          server_name: "{{ ansible_host }}"